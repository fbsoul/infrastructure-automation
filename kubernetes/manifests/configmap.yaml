apiVersion: v1
kind: ConfigMap
metadata:
  name: webapp-config
  namespace: default
data:
  config-note.txt: |
    ==================================================
    ENVIRONMENT INFO: K3S PRODUCTION LAB CLUSTER
    Configured dynamically via Kubernetes ConfigMaps.
    ==================================================

