apiVersion: v1
kind: Service
metadata:
  name: my-webapp-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: custom-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 32080
